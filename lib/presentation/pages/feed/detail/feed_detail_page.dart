import 'package:flutter/material.dart';

class FeedDetailPage extends StatelessWidget {
  const FeedDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Feed Detail Page'),
      ),
    );
  }
}
